// Copy to Config.local.xcconfig (gitignored) and fill in real values.
// xcconfig treats // as a comment, so keep the $() split in the URL.

NEXT_PUBLIC_SUPABASE_URL = https:/$()/YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY = YOUR_SUPABASE_ANON_KEY
